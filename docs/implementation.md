# Implementation

MVP: Voice artifact → Episode JSONL → Event JSONL → Relation candidate → Human confirmation → static View.

GitHub stores Canon and JSONL. GitHub Actions builds derived data. gh aw orchestrates agentic operations.
